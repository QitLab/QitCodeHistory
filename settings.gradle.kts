pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        jcenter()
    }
    enableFeaturePreview("VERSION_CATALOGS")
}

rootProject.name = "C++_Learning"
include(":app")
 